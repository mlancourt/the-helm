{
  "name": "the-helm",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --config worker/wrangler.toml --port 8787",
    "deploy": "wrangler deploy --config worker/wrangler.toml",
    "mock": "node tools/make-mock-data.js > docs/mock/helm-data.json",
    "test": "node tools/test-worker.js"
  },
  "author": "",
  "license": "UNLICENSED",
  "description": "The Helm — personal homepage: Cloudflare Worker + static PWA",
  "devDependencies": {
    "wrangler": "^4.133.0"
  },
  "private": true
}
